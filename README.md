Very basic, will break.

"Tested" usage
Install > enable bluetooth on prompt > Scan > enable location on prompt > select bluetooth device

Made to scan for specific BLE UUID which works with Polar H10.
